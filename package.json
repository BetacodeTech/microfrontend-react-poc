{
  "name": "microfrontend-poc",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "react-project": "serve react-project/build -p 9090 &",
    "html-project": "serve -p 9091",
    "start": "cd react-project && npm run build && cd .. && npm run react-project && npm run html-project",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.5.2",
    "jquery": "^3.5.1",
    "serve": "^11.3.2"
  }
}
